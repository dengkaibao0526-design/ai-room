name: Android Debug APK

on:
  pull_request:
    paths:
      - "android/**"
      - "components/MobileSpatialSensor.jsx"
      - "components/NativeShareBridge.jsx"
      - "app/components/chat/MobileTools.js"
      - "app/install/**"
      - "app/install-webclip.css"
      - "app/layout.js"
      - ".github/workflows/android-debug.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 16 platform
        run: yes | sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Install Gradle 8.11.1
        run: |
          curl -fsSL https://services.gradle.org/distributions/gradle-8.11.1-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d "$HOME"
          echo "$HOME/gradle-8.11.1/bin" >> "$GITHUB_PATH"

      - name: Assemble debug APK
        id: assemble
        continue-on-error: true
        working-directory: android
        shell: bash
        run: |
          set -o pipefail
          gradle --no-daemon :app:assembleDebug 2>&1 | tee "$RUNNER_TEMP/android-build.log"

      - name: Upload Gradle build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xiaokb-android-build-log
          path: ${{ runner.temp }}/android-build.log
          if-no-files-found: error

      - name: Upload debug APK
        if: steps.assemble.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: xiaokb-android16-vivo-xiaomi-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Publish APK to website downloads
        if: steps.assemble.outcome == 'success' && github.event_name == 'pull_request' && github.event.pull_request.head.ref == 'agent/unified-download-center'
        shell: bash
        run: |
          mkdir -p public/downloads
          cp android/app/build/outputs/apk/debug/app-debug.apk public/downloads/XiaoKB-Android16-Motion120.apk
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/downloads/XiaoKB-Android16-Motion120.apk
          if git diff --cached --quiet; then
            echo "Website APK is already current."
          else
            git commit -m "build: publish Android 16 Motion120 APK"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi

      - name: Fail when Android compile fails
        if: steps.assemble.outcome == 'failure'
        run: exit 1
